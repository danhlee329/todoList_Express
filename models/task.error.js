const TaskErrorCodes = {
    UUID_REQUIRED: 'Task id must be a valid UUID',
    NAME_STRING_REQUIRED: 'Task name must be a non-empty string',
    COMPLETED_TYPE: 'Task complete value must be a boolean',
}

module.exports = TaskErrorCodes;